---
import { getLangFromUrl, useTranslations } from '@/i18n/utils';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<footer class="flex lg:z-20 justify-center items-center h-14 mt-1 w-full">
  <small class="flex gap-1">{t('footer.text')}</small>
</footer>
