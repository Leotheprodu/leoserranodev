---
import MainLayout from '../layouts/MainLayout.astro';
import personalProjects from '../data/personalProjects.json';
import { Icon } from 'astro-icon';
---

<MainLayout
    title="Projects"
    description="Find here information about the projects I have developed."
>
    <section class="flex flex-col gap-4">
        <h1>Projects</h1>

        <p>
            Currently, I have several ongoing projects, both practical and in
            progress. Many of them can be found on my GitHub profile. However,
            I'd like to share with you the ones I'm actively working on
        </p>

        <h2>Personal Projects</h2>
        {
            personalProjects.map((project) => (
                <div class="dark:bg-slate-900 rounded-md p-5 border relative  dark:border-slate-700/25 shadow-md flex flex-col gap-4 overflow-hidden">
                    <span class="uppercase text-indigo-100 dark:text-indigo-400 bg-slate-800 absolute right-[-2.7rem] top-[2.1rem] px-10 rotate-45 shadow-2xl">
                        {project.type}
                    </span>
                    <h3>{project.title}</h3>
                    <p>{project.description}</p>
                    <div class="flex flex-col items-center justify-center gap-5">
                        <h4>Technologies</h4>
                        <ul class="flex gap-5 flex-wrap items-center justify-center">
                            {project.technologiesUsed.map((technology) => (
                                <li class=" text-indigo-900 dark:text-indigo-500 flex flex-col justify-center items-center">
                                    <Icon
                                        class="fill-slate-600 dark:fill-slate-300 transition-all ease-out duration-500 "
                                        name={technology.icon}
                                        width="20"
                                    />
                                    <p>{technology.name}</p>
                                </li>
                            ))}
                        </ul>
                    </div>
                    <div class="flex flex-col items-center justify-center gap-5">
                        <h4>Links</h4>
                        <div class="flex gap-5 flex-wrap items-center justify-center">
                            {project.links.map((link) => (
                                <a
                                    class="border-b border-transparent hover:border-indigo-700 transition-all duration-300 relative"
                                    href={link.url}
                                >
                                    {link.text}

                                    <span class="absolute bottom-0 left-0 w-0 bg-indigo-700 h-0 border-t-2 border-indigo-700 transition-all duration-300" />
                                </a>
                            ))}
                        </div>
                    </div>
                </div>
            ))
        }

        <h2>Client Projects</h2>

        <h2>Future Projects</h2>

        <h2>Practice Projects</h2>
    </section>
    <style>
        a:hover .absolute {
            width: 100%; /* Ancho máximo para la animación */
            height: 2px; /* Grosor del borde inferior */
        }
    </style>
</MainLayout>
